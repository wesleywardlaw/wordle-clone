# Wordle Clone

Wordle clone that loads new correct word each time the app loads. Checks that guesses are in either a list of potential wordles or a valid word list.

Vite app, starts with `npm run dev`.
